[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm ci", "cd frontend && npm ci", "cd backend && npm ci"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "node backend/server.js"
